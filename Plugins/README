
Each plugin can define some informations about itself:
   * Its execution priority
   * Its prefered size
   * Its tip
   * Its title
   * Its CSS style
   * Its default ACLs for the admin/teacher/author/student roles.
   * Its container.
   * If it is boxed.
   * An evaluation function that may:
      * Define one part of the page.
      * Modify other page part.
      * Modify the student state or session.
      * Modify the statistics.
      * Generate final HTML from all the page part.
      * Implement authentication.

When an HTTP request come the plugins are called from the first to the last.
If the user do not have the access right to the plugin, it is not executed.

A question directory can have its own 'Plugins' directory in order
to have a specific display for the questionnary. NOT YET IMPLEMENTED

TODO Absolute :
	'deconnexion' ==> RECONNEXION marche pas
	'session_start' pas affiché quand on fait 'Visibilité'
	Le recharge question, n'est pas immédiat sur l'affichage,
	enlever evaluate_answer et remplacer par plugin ? (pour cours Python)
	finir answered_other
	Probleme redirection UE constant ?
	FINIR acls
		ACL : hideable
		Ajouter/Enlever une ACL	à quelqu'un.
		Ajouter/Enlever un role	à quelqu'un.
	Faire disparaitre 'configuration' au profit de 'session' ?
	plugin : casauth
	plugin : language

TODO maybe :
	Extract 'auto focus' from 'questions.py' ?
	Split 'tips' from 'page.py' ?







